<%= turbo_stream.replace @user, partial: "admin/dashboards/user", locals: { user: @user } %>
<%= turbo_stream.replace "user_statistics", partial: "admin/dashboards/user_statistics", locals: { users_by_role: @users_by_role } %>
